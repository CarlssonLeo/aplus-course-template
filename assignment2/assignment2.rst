Assignment 2
============

This is the second assignment, which is a slightly harder assignment. Write a function called ``summary_statistics`` that returns the a python dictionary containing the sample mean, sample standard deviation, the minimum value, and the maximum value.

The keys for this dictionary should be ``mean``, ``std``, ``min`` and ``max``. 

.. submit:: assignment2 10
    :config: assignments/summary_statistics/config.yaml
    :title: Summary Statistics

    Implement the ``summary_statistics`` function as described above. 